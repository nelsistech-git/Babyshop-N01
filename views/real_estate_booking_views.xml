<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_real_estate_booking_tree" model="ir.ui.view">
        <field name="name">real.estate.booking.tree</field>
        <field name="model">real.estate.booking</field>
        <field name="arch" type="xml">
            <tree string="Bookings" decoration-success="state=='confirmed'" decoration-muted="state=='cancelled'">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="unit_id"/>
                <field name="salesperson_id"/>
                <field name="booking_date"/>
                <field name="final_price"/>
                <field name="state" widget="badge"
                       decoration-success="state=='confirmed'"
                       decoration-warning="state in ('draft','submitted','approved')"
                       decoration-muted="state=='cancelled'"/>
            </tree>
        </field>
    </record>

    <record id="view_real_estate_booking_form" model="ir.ui.view">
        <field name="name">real.estate.booking.form</field>
        <field name="model">real.estate.booking</field>
        <field name="arch" type="xml">
            <form string="Property Booking">
                <header>
                    <button name="action_submit" type="object" string="Submit" class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve" class="oe_highlight"
                            invisible="state != 'submitted'"/>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight"
                            invisible="state != 'approved'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state == 'cancelled'"/>
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft"
                            invisible="state in ('draft','confirmed')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved,confirmed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="customer_id" required="1"/>
                            <field name="unit_id" required="1"/>
                            <field name="project_id" readonly="1"/>
                            <field name="salesperson_id"/>
                        </group>
                        <group>
                            <field name="booking_date"/>
                            <field name="sale_price"/>
                            <field name="additional_charges"/>
                            <field name="discount"/>
                            <field name="final_price" readonly="1" class="fw-bold"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="booking_amount"/>
                            <field name="payment_method"/>
                        </group>
                        <group>
                            <field name="sale_agreement_id" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>
                        <page string="Documents">
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_real_estate_booking_search" model="ir.ui.view">
        <field name="name">real.estate.booking.search</field>
        <field name="model">real.estate.booking</field>
        <field name="arch" type="xml">
            <search string="Search Bookings">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="unit_id"/>
                <field name="salesperson_id"/>
                <filter string="Confirmed" name="filter_confirmed" domain="[('state','=','confirmed')]"/>
                <group expand="0" string="Group By">
                    <filter string="Project" name="group_project" context="{'group_by': 'project_id'}"/>
                    <filter string="State" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Salesperson" name="group_salesperson" context="{'group_by': 'salesperson_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_real_estate_booking" model="ir.actions.act_window">
        <field name="name">Bookings</field>
        <field name="res_model">real.estate.booking</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_real_estate_booking_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first Booking</p>
        </field>
    </record>

</odoo>

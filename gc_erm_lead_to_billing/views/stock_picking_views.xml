<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_picking_form_gc_erm" model="ir.ui.view">
        <field name="name">stock.picking.form.gc.erm</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='origin']" position="after">
                <field name="gc_work_order_id" readonly="1"
                       invisible="not gc_work_order_id"
                       groups="gc_erm_lead_to_billing.group_gc_erm_user"/>
                <field name="gc_team_id" invisible="not gc_work_order_id"
                       groups="gc_erm_lead_to_billing.group_gc_erm_user"/>
                <field name="gc_customer_id" readonly="1" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="vpicktree_gc_erm" model="ir.ui.view">
        <field name="name">stock.picking.tree.gc.erm</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="gc_work_order_id" optional="hide"
                       groups="gc_erm_lead_to_billing.group_gc_erm_user"/>
                <field name="gc_team_id" optional="hide"
                       groups="gc_erm_lead_to_billing.group_gc_erm_user"/>
            </field>
        </field>
    </record>

</odoo>
